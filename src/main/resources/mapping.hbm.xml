<?xml version = "1.0" encoding = "utf-8"?>
<!DOCTYPE hibernate-mapping PUBLIC
        "-//Hibernate/Hibernate Mapping DTD//EN"
        "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd">

<hibernate-mapping>
    <!--
    <class name = "com.prometheus.bankntity.User" table = "students">

        <meta attribute = "class-description">
            This class contains the employee detail.
        </meta>

        <id name = "id" type = "long" column = "id">
            <generator class="native"/>
        </id>

        <property name = "firstName" column = "first_name" type = "string"/>
        <property name = "lastName" column = "last_name" type = "string"/>
        <property name = "email" column = "email" type = "string"/>

    </class>
    -->
    <!--
    <class name="com.prometheus.bankntity.Lecturer" table="lecturers">
        <id name="id" type="long" column="id">
            <generator class="native"></generator>
        </id>

        <property name="firstName" column="first_name" />
        <property name="lastName" column="last_name" />
        <property name="email" column="email" />
        -->
        <!-- One to Many mapping -->
        <!--<set name="courses" table="courses"
             inverse="true" lazy="true" fetch="select" cascade="all">
            <key>
                <column name="lecturer_id" />
            </key>
            <one-to-many class="com.prometheus.bankntity.Course" />
        </set>

    </class>

    <class name="com.prometheus.bankntity.Course" table="courses">
        <id name="id" type="long" column="id">
            <generator class="native"></generator>
        </id>

        <property name="name" column="name" />
        -->
        <!-- One to One mapping -->
        <!--<one-to-one name="details" class="com.prometheus.bankntity.CourseDetails" cascade="save-update"/>
    </class>

    <class name="com.prometheus.bankntity.CourseDetails" table="course_details">
        <id name="id" type="long" column="id">
            <generator class="native"></generator>
        </id>

        <property name="department" column="department" />
        <property name="credits" column="credits" />

    </class> -->
</hibernate-mapping>